name: stack-dev-helpers

networks:
  my-network-proxy:
    external: true

volumes:
  backup:
  stack-dev-helpers-cloudbeaver-workspace:
    external: true
  stack-dev-helpers-pgadmin-data:
    external: true

services:
  cyberchef:
    restart: unless-stopped
    image: ghcr.io/gchq/cyberchef:11.2.0
    networks:
      - my-network-proxy
    expose:
      - 80
    labels:
      virtual.caddyfile: |
        http://cyberchef.localhost {
          reverse_proxy cyberchef:80 {
          }
        }
      virtual.homefile: |
        <div>
          <a class="shadow" href="http://cyberchef.localhost">cyberchef</a>
          <small>http://cyberchef.localhost</small>
        </div>

  cloudbeaver:
    restart: unless-stopped
    image: dbeaver/cloudbeaver:26.1.1
    networks:
      - my-network-proxy
    expose:
      - 8978
    volumes:
      - stack-dev-helpers-cloudbeaver-workspace:/opt/cloudbeaver/workspace
    labels:
      virtual.caddyfile: |
        http://cloudbeaver.localhost {
          reverse_proxy cloudbeaver:8978 {
          }
        }
      virtual.homefile: |
        <div>
          <a class="shadow" href="http://cloudbeaver.localhost">cloudbeaver</a>
          <small>http://cloudbeaver.localhost</small>
        </div>

  pgadmin:
    restart: unless-stopped
    image: dpage/pgadmin4:9.15.0
    user: root
    networks:
      - my-network-proxy
    expose:
      - 80
    environment:
      PGADMIN_DEFAULT_EMAIL: pgadmin@email.com
      PGADMIN_DEFAULT_PASSWORD: r74Qsg7jdwUfryM
      GLOBALLY_DELIVERABLE: false
    volumes:
      - stack-dev-helpers-pgadmin-data:/var/lib/pgadmin
    labels:
      virtual.caddyfile: |
        http://pgadmin.localhost {
          reverse_proxy http://pgadmin:80 {
          }
        }
      virtual.homefile: |
        <div>
          <a class="shadow" href="http://pgadmin.localhost">pgadmin</a>
          <small>http://pgadmin.localhost</small>
        </div>
